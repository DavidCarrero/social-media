import { PasswordStrength } from './password-strengt.hvalidator';

describe('PasswordStrength', () => {
  it('should create an instance', () => {
    expect(new PasswordStrength()).toBeTruthy();
  });
});
